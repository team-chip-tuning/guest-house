spring:
  datasource:
    name: guest-house
    url: jdbc:postgresql://localhost:5432/guest-house
    username: postgres
    password: mysecretpassword
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQL9Dialect
    show-sql: false
    hibernate:
      ddl-auto: update